function addItem(todoItem) {
  let todoItems = [];

  todoItems = todoItem;
  return todoItems;
}

function removeItem(todoItem) {
  let todoItems = ["Grocery shop"];

  todoItems.splice(0,1);
  return todoItems;
}
